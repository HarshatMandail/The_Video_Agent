# Assets

Place a `cursor.png` file here — a 32x32 white arrow cursor with transparent background and thin dark outline.

If no `cursor.png` is present, the system will auto-generate a basic one using FFmpeg.

For best results, use a real macOS/Windows-style cursor exported from your OS at 32x32px with alpha transparency.
